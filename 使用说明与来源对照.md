# Craft 技能套件：使用与融合来源

这次新增 18 个独立 Skill：1 个导航 + 17 个专业能力。每个有自己的方法、输入、边界和交付条件；setup 另带 8 个条件模板及一份全套共用的项目协议。专业技能不依赖原 Matt 或学长 Skill 安装，但套件内部共享协议和专项方法，应整套保留相对目录。

## 快速开始

在实际项目根目录运行（本次仅安装技能，没有初始化你的业务仓库）：

```text
$craft-project-setup
检查这个项目，给我最小配置方案，沿用已有任务系统，按需要建立 checklist 和 shared-context；确认布局后再写。
```

不确定用哪个：

```text
$craft-suite-router
帮我分析这个需求，先只读，遇到影响实现的决策再问我。
```

明确任务直接用，不必经过导航：

```text
$craft-discovery 先把这个权限需求问清楚，不改任何文件。
$craft-task-planning 方案已确认，请写成验收标准和可独立执行的任务，暂不实现。
$craft-implementation 按已确认方案实现这个接口，补必要测试，保留无关改动。
$craft-debugging 只读分析这些线上日志，暂不写脚本或修改服务。
$craft-review 检查当前改动与指定基点的差异，只报告问题。
$craft-release 只准备版本和验证材料，不发布、不提交。
```

导航会读取一个当前专业方法并执行，必要时再读其专项引用；不是一次加载18个全文。直接调用不需要先跑完整setup。普通小改动跳过建看板/写规格。自然语言实现授权已经足够时，不在每个阶段重复询问；旧任务状态本身不授权。

## 文件怎样选择

- 有GitHub/GitLab等任务系统：它拥有唯一任务正文和状态，仓库可增加约束/证据；默认不新增另一套Kanban。
- 本地长期项目：可选kanban、适用维护清单和findings；术语/ADR有实际内容再建立。
- 已有自定义规范：映射原位置，不擅自搬家。
- 小项目：配置入口即可，task_source=none，计划在聊天里。

项目选择记录在 `docs/agents/craft.md`，所有专业技能读取它。协议本身在 `craft-project-setup/references/project-contract.md`。setup会保留已存在的AGENTS/CLAUDE内容，只更新一个配置块。Build wrapper只有独立Git历史/外置工作流确有需要且用户确认时使用。

信息归属：术语→glossary；架构取舍→已有决策owner/ADR；长期约束→checklist；调查证据→findings；当前问题和进度→唯一任务owner。已有checklist中的权威决策不会被强制搬到ADR。

## 18 个 Skill 的来源与实质改造

下面的“来源”是实际查阅材料后的改写归属，不是声称每个原Skill的全部能力都被无损复制。

| 新 Skill | 用途 | Matt 来源 | 学长来源 | 融合后的取舍 |
|---|---|---|---|---|
| `craft-suite-router` | 总导航 | ask-matt | 按需加载 AGENTS/checklist 的入口思想 | 只读取当前方法；支持真正读取并执行子Skill，不依赖不存在的Skill API |
| `craft-project-setup` | 项目初始化 | setup-matt-pocock-skills | SKILL.md 与 templates 全套布局/维护规则 | 单一任务owner，按项目选择模板；保留已有文件，支持重复运行 |
| `craft-discovery` | 需求澄清 | grilling、grill-with-docs | discussion 阶段和用户决策边界 | 事实先查，决策前沿追问；领域记录转给明确的知识owner |
| `craft-context-modeling` | 领域建模 | domain-modeling 与 CONTEXT/ADR 格式 | checklist 决策与 shared-context 边界 | 术语、决策、执行规则各有一个权威来源，避免ADR/checklist重复 |
| `craft-wayfinding` | 大型决策探索 | wayfinder | 任务树和 shared-context 证据 | 决策图与实现任务分开；去除硬性一会话只解决一个问题 |
| `craft-task-planning` | 规格与任务规划 | to-spec、to-tickets | kanban 状态、树与引用维护 | 规格和验收写入一个任务owner；tracker模式不复制本地Kanban |
| `craft-triage` | 外部需求分诊 | triage | 任务状态不代表执行授权 | 核实报告、统一角色、补齐brief；外部评论/关闭单独受授权约束 |
| `craft-architecture` | 架构与接口设计 | codebase-design、improve-codebase-architecture | 领域 checklist 与已确认决策 | 保留深模块和边界分析，不强制术语改名/HTML报告/凭空抽象 |
| `craft-research` | 证据调研 | research | shared-context findings 模板 | 结论带版本/来源，区分事实和推断；只读时直接交付聊天 |
| `craft-prototype` | 原型实验 | prototype、阶段交接思想 | findings 与任务回链 | 实验回答一个问题，保留可复现证据；不把原型成功当生产证明 |
| `craft-implementation` | 开发实现 | implement、tdd、code-review | change-sop、任务树和知识收尾 | 直接执行已授权工作；小任务不建流程，测试/审查按需深入 |
| `craft-testing` | 测试设计与TDD | tdd、tests.md、mocking.md | 验证清单 | 公共边界+独立预期值+真实红绿；不为文案修改制造无价值测试 |
| `craft-debugging` | 故障与性能诊断 | diagnosing-bugs | findings、验证与任务记录 | 保留可证伪假设/失败反馈；只读/不可复现可输出有限诊断 |
| `craft-review` | 代码审查 | code-review | 适用checklist、来源证据 | 固定基点，Contract/Craft双轴；按用户证据模板，风险公开不等于审查通过 |
| `craft-merge-resolution` | 合并冲突 | resolving-merge-conflicts | change-sop 与意图/决策证据 | 逐块保留双方意图；去掉全量stage和默认commit，保留用户工作 |
| `craft-handoff` | 交接与协作 | handoff、ask-matt phase boundaries | kanban 连续性与状态 | 交接携带revision/权限/验证；接手重查，允许中途独立子任务并行 |
| `craft-release` | 发布准备与验证 | 实现/验证/交接原则（无对应Matt专用发布Skill） | BuildKodex/.agents/skills/release-kodex | 泛化精确revision/资产校验/发布回读；删除私人SSH主机、Java25、四平台等专属配置 |
| `craft-learning-loop` | 项目复盘 | retro、writing-for-agents | checklist/shared-context 知识沉淀 | 证据驱动改一个规则/检查；不自动写全局记忆，不另建第二个学习数据库 |

## 哪些保留，哪些没有照搬

保留：决策前沿追问、领域词汇、真实证据、垂直切片、可证伪调试、双轴审查、任务状态与维护规则、来源可追溯的知识沉淀。

调整：不强制新建所有目录；不同时维护tracker与Kanban正文；不把一切写成ADR；不让每个阶段重复确认已授权工作；不按固定数量制造假设；不以文档数量/字数评判质量；不把原型“可丢弃”理解为必须删掉。

未迁移学长私人环境：idea、acodespace、kodex-home、remote-macbook的专用操作与私有路径。也没有覆盖Matt的课程创作、语言专项迁移、基础设施凭证向导等所有用途。这套定位是日常编程主线及必要配套，不是原所有技能的无损全集。

两次旧尝试 `engineering-workflow`、`craft-engineering` 仍保留，但不属于这18项，也不是任何新Skill的依赖。本次没有擅自删除/禁用Matt、学长或旧试作。因而安装总数确实增加；没有宣称已清理重叠项或证明模型能力提升。

## 验证与限制

18项通过官方quick_validate结构检查，并核对内部相对链接、唯一名称、UI元数据及模板存在性。独立Agent在隔离假仓库执行setup：保留用户AGENTS内容、复用GitHub任务owner、不创建Kanban/空CONTEXT；再次setup不重复写入。只读discovery场景保持文件集合不变。

还做了小改动直达实现、线上只读日志无法复现、必需集成检查受阻三个场景的指令推演，发现并修正task tree固定化、只读复现脚本和过早停止等问题。这些是有限行为验证，不是全部18项的真实项目实战测试；未执行真实发布、远程Issue修改或所有编程语言工具链。后续实际质量需按你的任务验证。

技能数量本身并不证明效果优劣；描述是否重叠、路由是否明确、实际加载内容和方法质量更关键。[官方技能说明](https://developers.openai.com/codex/skills/)说明按需读取完整正文。套件默认保持可发现性；没有修改你的全局skills启用配置。

## 原始来源

- Matt已安装快照：`Matt skills snapshot (local source used for this build)`。来源版本以本次读取文件SHA-256为准，不声称当前GitHub最新内容一致。
- [学长workflow](https://github.com/Stream29/checklist-kanban-context-workflow-skill)：SKILL.md和templates。
- [BuildKodex](https://github.com/Stream29/BuildKodex)：AGENTS、checklist维护规范和release-kodex。
- 编写规范：本机 Codex skill-creator 和 writing-for-agents 规范。
- 你的要求：按生态位融合独立技能、总导航按需访问、保留只读和授权边界。

完整指纹见 SOURCE-MANIFEST.json；验证记录见 VALIDATION.md。导出包只含本套新文件及说明，不含源仓库、凭据、旧Skill或业务代码。
